package com.example.movieservice.Repository;

import org.springframework.data.jpa.repository.JpaRepository;
import com.example.movieservice.Entity.DealTerm;

public interface DealTermRepository extends JpaRepository<DealTerm, Long> {}
